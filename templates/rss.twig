---
permalink: /rss.xml
---
<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0">
    <channel>
        <title>{{ config.title }}</title>
        <link>{{ config.base_url }}/</link>
        <description>{{ config.description }}</description>
        {% for year, postsOfYear in posts %}
        {% for post in postsOfYear %}
            <item>
                <title>{{ post.meta.title|raw }}</title>
                <link>{{ post.post_url|raw }}</link>
                <guid>{{ post.post_url|raw }}</guid>
                <pubDate>{{ post.meta.date|date('D, d M Y') }}</pubDate>
                <description>{{ post.meta.title|raw }}</description>
            </item>
        {% endfor %}
        {% endfor %}
    </channel>
</rss>
